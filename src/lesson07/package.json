{
  "name": "lesson07_series",
  "version": "0.0.1",
  "scripts": {
    "test": "jest --testPathPattern=__tests__",
    "build": "mkdir -p ./dist && cp ./src/series.js ./dist/series.js"
  },
  "dependencies": {},
  "devDependencies": {
    "jest": "^22.4.3"
  }
}
